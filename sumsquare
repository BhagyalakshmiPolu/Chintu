//sum of squares of individual digits of given number//
#include<stdio.h>
void main()
{
    int r,n,sum;
    scanf("%d",&n);
    sum=0;
    while(n!=0)
    {
        r=n%10;
        sum=sum+r*r;
        //for cube we use r*r*r in above statement
        n=n/10;
    }
    printf("%d",sum);
}
