//sum of indiviudual digits in given numbers//
#include<stdio.h>
void main()
{
    int n,r,sum;
    scanf("%d",&n);
    sum=0;
    while(n!=0)
    {
        r=n%10;
        sum=sum+r;
        n=n/10;
    }
    printf("%d",sum);

}
